// Object.keys(object) ! Returns object keys
// Object.freeze(object) ! Freezes the object
// Object.defineProperty(this, 'object', { configuration }) ! Define an object properties
// Object.defineProperties(this, { obj1: { config }, obj2: { config }, ... }) ! Define multiple obj props
// { ...object } ! Copies an object to a variable without linking them

// Object.assign({ }, object to be copied, { other keys to add }); ! Copies other object to a variable
// Object.getOwnPropertyDescriptor(object, 'property') ! Returns the object properties
// Object.values(object) ! Returns object values
// Object.entries(object) ! Returns an array containing an array for each key + value in the object
